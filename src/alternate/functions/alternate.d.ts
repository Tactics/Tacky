import { AlternateItem } from "../contracts/alternate";
export declare const LookupAlternate: ({ name, alternates, }: {
    name: string;
    alternates: Array<AlternateItem>;
}) => AlternateItem | undefined;
